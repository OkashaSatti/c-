#include<iostream>
using namespace std;
int main()
{
    int num,sum;
    cout<<"Input a number: ";
    cin>>num;
    for(int i=1; i<=num;i++)
    {
    sum=sum+i;
    }
    cout<<"The total sum of "<<num<<" is: "<<sum;
}
